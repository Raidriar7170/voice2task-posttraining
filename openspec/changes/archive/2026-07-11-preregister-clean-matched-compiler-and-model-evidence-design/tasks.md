## 1. Freeze authoritative inputs and RED design contracts

- [x] 1.1 Inventory and hash the archived compiler-causal audit summary, main audit spec, `CONTEXT.md`, public manifest, split-integrity summary, and expressly allowed aggregate lockbox status artifacts; assert that raw lockbox rows, private predictions, corpora, caches, adapters, checkpoints, and logs are absent from the input whitelist. Evidence: exact 10-file whitelist, frozen hashes/anchors, and exact/prefix denylist are covered by focused tests.
- [x] 1.2 Add RED tests for the design schema, the single canonical execution-binding inventory with every value exactly `UNBOUND_BY_DESIGN`, exact terminal statuses, readiness-state ordering, machine-readable blocked reasons, and all false execution/claim flags. Evidence: TDD RED recorded before the 29-field/status/lifecycle/flags implementation; focused GREEN passes.
- [x] 1.3 Add RED tests for one acquisition plan, two family-disjoint sealed partitions, pre-acquisition rule freeze, materialization-time family assignment before row/gold/outcome access, independent one-look consumption, lineage/disjointness rules, no-row guarantees, and clean/natural-ASR claim boundaries. Evidence: TDD RED on missing population design followed by focused GREEN for future-only partition and no-row contracts.
- [x] 1.4 Add RED tests for separate compiler/model cards, full compiler ITT, paired within-record contrasts plus family-clustered uncertainty, exactly-one model intervention, matched arms, at least three all-assigned paired seeds, seed-level ITT failure coding, family/seed uncertainty, power/ICC/correlation/attrition assumptions, invariants, negative controls, and exactly one unexecuted next recommendation. Evidence: TDD RED on missing cards/power/hard stops followed by focused GREEN.

## 2. Implement the deterministic design-only contract

- [x] 2.1 Implement a read-only design helper with an explicit normalized source whitelist, frozen expected hashes, exact/prefix denylist handling, strict in-repo regular-file/no-symlink resolution, one bytes snapshot for hash plus source-anchor checks, and no broad directory discovery. Evidence: external final/parent symlinks and missing/drifted/rejected paths fail before target reads; build reads each of 10 sources exactly once.
- [x] 2.2 Populate the clean acquisition and lineage schema, the exact canonical `UNBOUND_BY_DESIGN` inventory, `compiler_system_evaluation` / `model_learning_evaluation` rule-freeze and future membership-assignment contracts, independent one-look states, and fail-closed readiness lifecycle without creating a row, family registry, membership artifact, or manifest. Evidence: report records both partitions as `NOT_MATERIALIZED` / `NOT_SEALED` and all artifact-created flags false.
- [x] 2.3 Populate the compiler/system preregistration card with identical raw-record arms, explicit `compiler_control=UNBOUND_BY_DESIGN` and `compiler_intervention=UNBOUND_BY_DESIGN` bindings, future named-arm completeness rules, full ITT denominator, paired within-record contrasts, family-clustered interval/multiplicity requirements, invalid/unsupported failure handling, guardrails, negative controls, and `system_compiler_transformation_effect` attribution. Evidence: compiler-card focused assertions pass.
- [x] 2.4 Populate the model-learning preregistration card with explicit unbound control/intervention/seed-list bindings, exactly-one-intervention enforcement, matched data/prompt/decoding/compiler/evaluator/budget/evaluation requirements, at least three all-assigned paired seeds, seed-level ITT failure coding and aggregation order, model-authored outcomes, and bundled-pipeline rejection rules. Evidence: model-card focused assertions pass with all-assigned seed ITT and bundled-pipeline rejection.
- [x] 2.5 Populate separate pre-outcome compiler/model MDE and sample-size contracts, target power/beta, family variance/ICC, paired-seed correlation, seed failure/attrition, family/seed hierarchical uncertainty, interval/multiplicity methods, invariant matrix, hard stops, terminal `DESIGN_ONLY` truth surface, and the single unexecuted `materialize-and-freeze-clean-compiler-model-evaluation-boundary-v1` recommendation. Evidence: uncertainty, invariant, hard-stop, exact-status, and single-recommendation assertions pass.
- [x] 2.6 Add a content-addressed phase-time `CONTEXT.md` snapshot and exact logical-path resolver for the archived causal audit; keep the archived JSON/Markdown bytes, logical source path/hash, status, and metrics unchanged while the new design binds live current context. Evidence: exact-whitelist, missing/non-regular, final/parent symlink and outside-redirect guards reject before reads; archived JSON/Markdown replay byte-identically from the 16,755-byte `2ffc67d8...1009f` snapshot.

## 3. Publish public-safe design evidence

- [x] 3.1 Generate byte-deterministic `summary.json` and `summary.md` under `reports/public-sample/clean-matched-causal-evidence-design/`, with source hashes, both cards, bound/unbound fields, lifecycle, uncertainty, invariants, negative controls, statuses, and false flags. Evidence: formal double render is byte-identical and focused renderer test passes.
- [x] 3.2 Update evidence-index and current-status navigation with `evidence_status=DESIGN_ONLY`, `decision=PREREGISTRATION_DESIGN_READY_EXECUTION_BLOCKED`, `clean_population_status=NOT_MATERIALIZED`, and `execution_readiness=false`, without a clean-evidence, causal-effect, or model-improvement headline. Evidence: JSON index parses with one `DESIGN_ONLY` entry and Markdown/current-status preserve blocked claims.
- [x] 3.3 Generate `docs/human-briefs/2026-07-10-preregister-clean-matched-compiler-and-model-evidence-design.html` from the final design bundle, source hashes, review findings, and validation evidence. Evidence: applied design-only brief links to both deterministic reports; 10 local links resolve and leak scan is clean.

## 4. Verify, review, and archive

- [x] 4.1 Assert no mutation of public seed/SFT/DPO/manifest, any lockbox artifact, prompts, decoding, schema, evaluator defaults, predictions, models, adapters, checkpoints, or runtime code; assert that no clean-row or executable-experiment artifact exists. Evidence: public seed/SFT/DPO/manifest and protected prompt/decoding/schema/evaluator hashes match the frozen boundary; no lockbox/data/model/runtime status drift, clean rows, or executable artifacts were introduced; outputs are limited to public-safe summaries and the content-addressed historical context snapshot.
- [x] 4.2 Run focused design tests, symlink/missing/hash-drift/pre-read source-policy tests, related causal-audit/renderer/evaluator tests, archived-report byte-identity checks, read-only public dataset/schema validation, the 2100-pair DPO check, deterministic regeneration, HTML-link validation, and public leak scans while comparing protected hashes around every potentially writing command. Evidence: 172 related tests pass; public validation reports 696 SFT rows and 2100 DPO pairs; old/new reports reproduce byte-identically; 10 HTML links resolve, the public leak scan has zero findings, and protected hashes remain unchanged.
- [x] 4.3 Run full pytest, `ruff check .`, focused Mypy plus the recorded full-Mypy baseline, `openspec validate --all --strict`, and `git diff --check`; preserve and document any active-change cleanup guards rather than weakening them. Evidence: full pytest reports 773 passed and only the four expected active-change/archive guards; Ruff passes; focused Mypy reports zero issues; full Mypy preserves the 39-error/5-file baseline while checking 29 source files; strict OpenSpec validates 13/13, the truth checker fails only on active-change count 1, and `git diff --check` passes.
- [x] 4.4 Complete independent read-only spec and code-quality reviews, resolve every Must Fix inside the design-only scope, rerun affected validation, and keep `PROTOCOL_FROZEN`, population materialization, compiler/model execution, training, and prediction out of scope. Evidence: final independent spec and code reviews both return Pass with no Must Fix, Should Fix, Nice to Have, or re-plan request; protocol freeze, population materialization, experiments, training, and prediction remain blocked and unexecuted.
- [x] 4.5 Archive only after the Human Brief and evidence are current; then rerun no-active-change truth-surface, full pytest, strict OpenSpec, leak, protected-hash, HTML-link, and diff checks. Evidence: delta spec synced to the new main capability spec and the change archived under the 2026-07-11 path with active changes at zero; post-archive full pytest reports 777 passed, current truth-surface passes, strict OpenSpec validates 13/13, public leak scan reports zero findings, four protected public-data hashes remain frozen, all 10 Human Brief links resolve, and `git diff --check` passes.
