## 1. Freeze the existing boundary and add RED contracts

- [x] 1.1 Record the current `DESIGN_ONLY` report/status, exact 29-field inventory, archived design/main-spec hashes, public seed/SFT/DPO/manifest hashes, lockbox aggregate hashes, and forbidden path prefixes before any potentially writing command; assert no canonical/promoted family registry, membership, or seal exists while permitting an apply-time private source frame only after it is recorded and validated.
- [x] 1.2 Add RED tests for the exact typed binding dossier, required authority/hash/derivation/no-access/review fields, 29/29 completeness, alias/conflict rejection, blocked-sentinel rejection, exactly-one model intervention, and paired-seed identity/minimum rules.
- [x] 1.3 Add RED tests for the single canonical private/public roots, trusted-root descriptor-relative open, missing/non-regular/final-symlink/parent-replacement/exchanged-symlink/outside-redirect/hardlink/identity-drift rejection, `O_NOFOLLOW`/`fstat`/`st_nlink=1`, bounded bytes/records, exact/prefix denial of public/lockbox/report/model/run/log inputs, zero reads on rejection, and forbidden row/gold/prediction/outcome/private-path fields.
- [x] 1.4 Add RED tests for separate compiler/model planning modes and statistical dossiers, unsupported point-estimate rejection, conservative-grid handling, paired/family/seed dependence, protocol double-render identity and freeze-before-first-frame-open chronology, monotonic S0-S3 lifecycle, exact S0/S1/S2/compromised matrices, success truth, mutation/artifact/one-look flags, and all no-experiment/clean-independent/row-clean/benchmark claims.
- [x] 1.5 Add RED tests for strict UTF-8 JSON/JSONL parsing, duplicate/unknown-key and non-finite rejection, canonical record/unit/root hashes, one-family-per-frame-record, exact-count/integer-quota semantics, deterministic registry, fully bound aggregate lockbox attestation schema, row-level checks remaining `PENDING_ROW_AUTHORING_GATE`, deterministic exactly-once assignment, shortfall/oversupply/stratum/overlap gates, one-generation atomic directory promotion, zero-access seals, and public/private schema separation.

## 2. Implement binding, power, and protocol-freeze gates

- [x] 2.1 Add a focused `clean_evaluation_boundary` module with typed models for the 29 binding dossiers, source contract/frame schema, statistical cards, lifecycle states, private registry/membership/seal, public attestations, and machine-readable blocker codes; do not modify compiler, decoder, prompt, schema, evaluator, or runtime behavior.
- [x] 2.2 Implement the exact binding validator and single authoritative binding inventory; require concrete public-safe authority/source hashes and no-access attestations, and keep `DESIGN_ONLY` with `ACQUISITION_SOURCE_UNAVAILABLE_OR_UNVERIFIABLE` or `BINDING_INCOMPLETE_OR_PLACEHOLDER` when inputs cannot be proven.
- [x] 2.3 Implement deterministic, outcome-free compiler/model power preflight with exactly one `EFFECT_TARGETED` or `CAPACITY_CONSTRAINED` mode per estimand, family/paired-seed dependence, all-assigned-seed failure coding assumptions, sourced sensitivity grids or worst-case bounds, and fail-closed unsupported-assumption results.
- [x] 2.4 Implement strict canonical protocol serialization and freeze-manifest generation over all binding/source/schema/exact-quota/partition/card/statistical/invariant/privacy/hard-stop inputs; before freeze validate only the source contract and declared expected digest without opening frame payload, prove byte-identical double render without runtime timestamps, then reject in-place post-freeze drift.
- [x] 2.5 Add explicit `voice2task-data` validation/materialization/seal-verification entry points or equivalent focused scripts that accept only named input files, never discover directories, return nonzero on blockers, and print no private path or membership data.

## 3. Materialize and seal the private family boundary conditionally

- [x] 3.1 Implement source-attestation review and the first source-frame payload open only after `PROTOCOL_FROZEN`; use the race/hardlink-safe verified descriptor and one bounded bytes snapshot, compare actual versus frozen expected digest before decoding, enforce one unique family candidate per record and canonical hashes, and reject forbidden ancestry/content, duplicates, drift, invalid strata/eligibility/provenance, or public/lockbox substitution before registry writes.
- [x] 3.2 Implement deterministic private family-registry staging after `PROTOCOL_FROZEN`, using opaque ids and root-hashable entries; report source/provenance and semantic-family checks while preserving exact/normalized/template disjointness as `PENDING_ROW_AUTHORING_GATE`.
- [x] 3.3 Implement verification of the complete protocol/source/frame/registry/lockbox/validator/reviewer/count/zero-row-output/digest-or-signature schema for `SEALED_AGGREGATE_ATTESTATION_ONLY`, with zero lockbox-row/member-hash reads in the materializer and fail-closed missing, unauthorized, mismatched, nonzero-overlap, or row-output handling.
- [x] 3.4 Implement `sha256-partition-by-stratum-v1` with versioned domain-separated length-prefixed inputs, the exact frozen integer quota matrix, and exactly-once membership; reject shortfall and oversupply, alternate-seed retries, iteration-order dependence, down-selection, reassignment, top-up, deletion, overlap, quota/minimum mismatch, and nondeterministic replay.
- [x] 3.5 Implement private staging of registry, two memberships, and seal inside one new generation directory; flush files/directory, require the same filesystem, publish with one non-overwriting directory rename, and leave both partitions `SEALED_NOT_ELIGIBLE` with zero access while stopping at `POPULATION_MATERIALIZED_AND_SEALED`.
- [x] 3.6 Implement bounded public blocker generation for every pre-promotion failure and compromised-boundary invalidation for early row/gold/outcome/one-look access or membership leakage; never promote or repair partial canonical artifacts.
- [x] 3.7 Select exactly one evidence path from real inputs: successful metadata-only materialization/sealing or an honest blocked result. If no independent acquisition source is supplied, stop at the blocked path without fabricating bindings, source data, registry, membership, or success evidence.

## 4. Publish deterministic aggregate-only evidence

- [x] 4.1 Generate deterministic `summary.json` / `summary.md`, a public protocol manifest, population-seal attestation, and lineage attestation under `reports/public-sample/clean-compiler-model-evaluation-boundary-v1/`, containing aggregate counts/root hashes/statuses only and no member ids, per-member hashes, membership, rows, private paths, or outcomes.
- [x] 4.2 Record truthful exact success or S0/S1/S2/compromised fields and mutation flags: successful boundary materialization/private registry/private membership may be true, while public/formal-training/lockbox mutation, evaluation-row creation, gold/outcome access, training, prediction, A100, experiment execution, one-look access, arm freeze, readiness, clean-independent evidence, row-clean/evaluated-benchmark status, and all improvement/release claims remain false; unavailable hashes use `NOT_AVAILABLE`.
- [x] 4.3 Update `CONTEXT.md`, current-status/evidence navigation, and machine-readable evidence index with the exact success or blocked truth surface; use existing index classification `CURRENT` on successful sealing or `BLOCKED` on a blocked phase, and do not rewrite historical design, compiler-audit, lockbox, or negative evidence.
- [x] 4.4 Generate `docs/human-briefs/2026-07-11-materialize-and-freeze-clean-compiler-model-evaluation-boundary-v1.html` from the final protocol/evidence/review/validation sources, explicitly distinguishing metadata-only family sealing from clean-row creation and experiment eligibility.

Apply evidence: the real repository path selected the honest S0 blocker because no independent source frame or reviewed binding packet exists. Temporary fixtures exercise S1/S2/S3 and successful metadata-only sealing without becoming project evidence. Focused boundary/navigation tests, Ruff, focused Mypy, strict OpenSpec, truth-surface checks, deterministic replay, public leak/link checks, and protected-hash comparisons are the verification surface; section 5 remains controller/reviewer work, and archive remains separately authorized.

## 5. Verify, review, and preserve the next boundary

- [x] 5.1 Run focused binding/source/path/statistical/protocol/registry/lineage/partition/seal/privacy/status tests, deterministic double materialization or double blocked replay, CLI nonzero/zero-output blocker checks, and zero-read assertions for rejected/lockbox paths. Evidence: the final clean-boundary suite is 102 passed; the deterministic/blocker/zero-read targeted replay is 5 passed; independent race replays cover protocol persistence, trust anchoring, seal co-tamper, exact generation contents, no-replace promotion, ancestor exchange rollback, and staging ownership.
- [x] 5.2 Run read-only public dataset/schema validation and the 2100-pair DPO check while comparing public seed/SFT/DPO/manifest, lockbox aggregate, prompt/decoding/schema/evaluator, and archived-report hashes before and after every potentially writing command. Evidence: public validation is 696 SFT rows / 2100 DPO pairs with zero failures, the DPO check reads all 2100 pairs, and the complete protected hash inventory remains byte-identical after evidence, Brief, and task writes.
- [x] 5.3 Run full pytest, `ruff check .`, focused Mypy plus the recorded full-Mypy baseline, `openspec validate --all --strict`, current truth-surface checks, public leak scans, Human Brief link validation, `git check-ignore`, `git ls-files`, `git status`, and committed-diff guards proving the canonical private root is ignored/untracked, plus `git diff --check`. Evidence: full pytest is 879 passed / 4 expected active-change archive-guard failures with no extra regression; Ruff passes; focused Mypy is 3 files / 0 issues; full Mypy remains the recorded 39 errors / 5 files baseline; OpenSpec strict is 14/14; explicit apply-mode truth checking passes while the default zero-active guard correctly remains red until archive; leak scan is clean, Human Brief links are 13/13, the canonical private root is absent/ignored/untracked/status-clean, and `git diff --check` passes.
- [x] 5.4 Complete independent read-only spec and code-quality reviews; resolve every in-scope Must Fix, rerun affected gates, and keep compiler/model arm construction, experiments, training, prediction, A100, row authoring, gold/outcome access, and automatic next-phase creation out of scope. Evidence: independent final spec and security reviews both returned `PASS` with Must Fix=0 after four bounded attack/fix rounds; no real private source, registry, membership, seal, row, arm, training, prediction, A100, one-look, or experiment was created or run.
- [x] 5.5 Archive only after the user separately authorizes archive and the Human Brief/evidence are current; then rerun no-active-change truth-surface, full pytest, strict OpenSpec, leak, protected-hash, private-boundary, link, and diff checks. Evidence: the user separately authorized archive; the 9-requirement delta was synchronized into the new main capability spec; the change was archived as `2026-07-11-materialize-and-freeze-clean-compiler-model-evaluation-boundary-v1`; post-archive full pytest is 883 passed, default no-active truth checking passes, strict OpenSpec is 14/14 with zero active changes, leak scan has zero findings, 24 protected hashes are unchanged, the canonical private root remains absent/ignored/untracked, Human Brief links are 13/13, and `git diff --check` passes.
