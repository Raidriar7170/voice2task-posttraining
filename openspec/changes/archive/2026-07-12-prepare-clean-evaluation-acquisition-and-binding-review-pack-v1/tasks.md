## 1. Freeze the S0 baseline and add RED contracts

- [x] 1.1 Record the archived clean-boundary S0 status, exact 29-field inventory, existing execution-validator/protocol-render behavior, archived five-file blocker hashes, public seed/SFT/DPO/manifest hashes, lockbox aggregate hashes, and canonical private-root absence/ignore/tracking state before any writing command.
  Apply evidence: captured the 29-field tuple, S0 five hashes, protected public/lockbox hashes, protocol/canonical/persisted hashes, mode `0600`, `validate_named_inputs` persisting exactly once, focused 4-test behavior baseline, and `PRIVATE_ROOT_ABSENT` plus ignored/untracked state before the first repository write.
- [x] 1.2 Add RED tests proving the generated binding catalog is derived from `EXECUTION_BINDING_FIELDS`, contains exactly the same 29 names in the same order, has no aliases or values, and renders byte-identically on repeated runs.
  Apply evidence: RED failed at import for the absent module; GREEN proves runtime derivation (including a monkeypatched inventory), exact 29/order parity, recursively value/alias-free entries, explicit unrecorded human-acceptance gates, and byte-identical canonical rendering.
- [x] 1.3 Add RED tests for the strict single-envelope schema and template: separate template/candidate-envelope and four `*-draft-v1` `$id` values, fixed JSON Schema draft, `additionalProperties=false`, `template_only=true`, nested `draft_only=true`, draft-field records below `proposed_bindings`/`proposed_fields` so no object combines a raw component/dossier key set with its raw value layout, machine-searchable `NOT_AN_EXECUTION_INPUT` and `NO_HUMAN_ACCEPTANCE_RECORDED`, every unsupplied field represented as `NOT_SUPPLIED`, no valid-looking fake hashes, and no standalone execution component output.
  Apply evidence: strict Draft 2020-12 template/candidate envelopes and four draft identities are covered recursively for closed objects, placeholder wrappers, non-executable markers, no fake hashes, standards-based schema checking, positive candidate conformance, and rejection of all five raw execution layouts under all three draft fields at direct, object, and array depths.
- [x] 1.4 Add RED tests for the exact empty-pack preparation truth, complete-candidate ceiling `STRUCTURALLY_COMPLETE_REVIEW_REQUIRED`, separate `supplied_binding_count` versus fixed `authoritatively_bound_binding_count=0`, fixed `human_acceptance_status=NOT_RECORDED`, `freeze_authorized=false`, `next_phase_eligible=false`, and `execution_readiness=false`, plus sanitized blocker/field-only diagnostics for malformed, placeholder, private-path, independence, power, and cross-component failures.
  Apply evidence: RED failed for the absent lint API and later for discarded placeholder/private surfaces and escaping ordinary exceptions; GREEN locks exact empty/complete result keys and truth matrices, fixed zero authority/acceptance/authorization/readiness, an exact diagnostic allowlist, and sanitized handling of malformed, missing/unknown/alias, recursive placeholder/private-path, hash, independence, power, cross-component, complexity, and arbitrary ordinary-Exception cases without swallowing BaseException.
- [x] 1.5 Add RED tests proving review lint accepts exactly one non-executable envelope, never accepts/exports four raw components, and the complete envelope, every extracted draft, every `proposed_*` map, and every nested object all fail unchanged through `freeze_protocol`, `validate_named_inputs`, the corresponding execution validator, and the existing CLI before any hash/file; also assert the internal draft mapping is not returned/serialized, zero source-frame/lockbox-row reads, zero canonical private/protocol/registry/membership/seal writes, and no source-frame, lockbox, generation, seal, row, model, one-look, training, prediction, A100, or experiment argument.
  Apply evidence: every unchanged candidate object fails all execution validators, freeze, named-input validation, and the existing CLI before persistence; a real temp-root `clean-boundary-validate` read also rejects the envelope before protocol creation. Explicit per-function counters prove one shared-seam call and zero execution/I/O calls even with the total-lint catch-all, while outputs contain no ephemeral components, protocol hash, converter/exporter, or generic `ok`.

## 2. Implement the pure validation and review-pack core

- [x] 2.1 Characterize the existing four-component accept/reject matrix, blocker precedence, exception types, canonical protocol bytes/hash, persistence call count, and file permissions, then extract one shared pure pre-freeze validation seam; keep protocol rendering/persistence outside it and prove every characterized behavior is unchanged.
  Apply evidence: added RED-first seam/precedence/purity and persistence characterization tests, including binding/source/compiler/model failure order, strata/count cross-component failures, deep input non-mutation, and historical acceptance of an empty structured binding value; `validate_pre_freeze_inputs` owns only existing validators plus cross-component checks, while freeze rendering/hash/persistence remain outside and existing accept/reject behavior stays unchanged.
- [x] 2.2 Add a focused dependency-free `clean_evaluation_review_pack` module that derives the canonical catalog from the execution inventory and models only template, checklist, schema, gap, lint, summary, and manifest data; do not duplicate execution field inventories or semantic validators.
  Apply evidence: at the 2.2 slice, the pure in-memory dependency-free module contained catalog/schema/template/checklist builders but intentionally no linter; immutable public execution-boundary accessors supplied every raw field inventory and the review-input root was derived from the canonical private root. The separately scoped linter was added under task 2.4; no filesystem writer, execution-component exporter, or lifecycle mutation exists.
- [x] 2.3 Implement deterministic generation of `binding-catalog.json`, `review-pack.schema.json`, one non-executable `review-pack.template.json` envelope, and `review-checklist.md`, including provider/reviewer and lockbox-validator/reviewer separation, ancestry exclusions, derivation evidence, applicability, zero-access attestations, statistical review, explicit human-acceptance gates, and instructions that the operator alone may copy the template into the fixed ignored review-input root; do not pre-approve values, create that root, or export raw components.
  Apply evidence: the four bounded artifacts render byte-identically in memory; the checklist includes a deterministic 33-row matrix for four components plus all 29 bindings, with provider/reviewer/evidence/applicability/zero-access fields not supplied and human acceptance/preapproval not recorded, plus both independence pairs, all ancestry exclusions, statistical review, and operator-only manual copy while the tool creates no root.
- [x] 2.4 Implement the pure single-envelope candidate linter and exact preparation/candidate truth surfaces; validate nested drafts only in memory, emit only allowlisted codes/section names/canonical missing fields, and never return raw components or infer external authenticity/human acceptance from labels, hashes, or self-reported approval fields.
  Apply evidence: the dependency-free one-argument linter validates every recursive draft surface with shared public placeholder/private-path semantics while locally treating empty dict/list as review placeholders without changing execution semantics; bounded cycle/depth/node traversal and a final ordinary-Exception sanitizer make it total. It maps values to local ephemeral components, calls only `validate_pre_freeze_inputs`, discards the mapping, caps valid candidates at review-required, and keeps self-declared approval/hash labels non-authoritative.
- [x] 2.5 Implement fixed-path publication and exact idempotency: require pre-existing stable-mode non-writable trusted root/ancestors/`reports/public-sample` and zero reserved-prefix siblings; mechanically verify an exact existing `0755` owner/GID-policy final plus seven bounded-read byte-identical regular `0644` single-link members as a no-op; reject changed/partial/redirected/unsafe state with no namespace, content, or capability-issued metadata mutation; and, before absent-path staging, preflight supported Darwin/Linux no-replace implementation/symbol availability with fixed unsupported code `REVIEW_PUBLICATION_NO_REPLACE_UNAVAILABLE`. Keep retained-descriptor staging at `0700` through all member writes/fsyncs, then immediately before promotion use that descriptor to set `0755`, verify same identity/effective UID/trusted-parent GID/no group-or-other write, fsync directory+parent, and atomically no-replace. A post-preflight competitor must receive no namespace, content, or capability-issued metadata mutation, normal success must leave zero reserved siblings, and the review path must never exchange, `unlinkat`, `rmdir`, destructively clean, create ancestors, or use an ordinary fallback. Filesystem-managed access-time changes caused solely by descriptor reads are the only no-mutation exclusion.
  Approved re-plan evidence: the user approved create-once/exact-idempotent review publication after independent review disproved the prior portable pathname-CAS premise. The earlier 316-pass combined and 71-pass focused checkpoints remain diagnostic history rather than acceptance because they predate this mechanically bounded contract. Task 2.5 stayed open at re-plan time pending RED/GREEN implementation; no CLI, real report bundle, navigation, or archive work was authorized by the artifact re-plan.
  Apply evidence: a dedicated fixed-path review writer now verifies the pre-existing safe namespace, exact seven-member bounded-read no-op, platform no-replace capability, retained-`0700` staging, final retained-FD `0755` transition, parent/directory fsync, competitor preservation, and zero reserved siblings without calling the legacy exchange, unlink, cleanup, rmdir, or ordinary-replace paths. Adversarial absence/ancestor exchange and final-scan exchange tests prove the last namespace relink occurs before first mutation or successful return; focused publication tests pass 26 cases and the combined boundary/review-pack files pass 342 cases.
- [x] 2.6 Implement recovery, outcome-classification, current-gate, and compatibility regressions: reserve `.review-pack-recovery-` plus at least 128 CSPRNG bits; prove transaction ownership from invocation-local name absence, retained creation FD, and stable linked/opened/`fstat`/relinked device/inode/type/UID/GID; use that retained FD to set or restore provably owned unpromoted staging to `0700`, verify same identity/effective UID/trusted-parent GID/mode `0700`, retain it, and return `REVIEW_PUBLICATION_RECOVERY_RETAINED`; perform no further pathname, content, or capability-issued metadata mutation and return `REVIEW_PUBLICATION_IDENTITY_UNCERTAIN` when proof fails; classify post-syscall wrapper errors by observation before any permitted recovery action, never downgrade a proven promoted final, and use `REVIEW_PUBLICATION_POST_SYSCALL_FAILURE` for that promoted outcome; and block success/current/task 3.2 with `REVIEW_PUBLICATION_RECOVERY_PRESENT` while any reserved-prefix sibling exists. Filesystem-managed access-time changes caused solely by descriptor reads are the only no-mutation exclusion. Keep automatic GC out of scope. For legacy `write_public_evidence`, add no threat/recovery semantics and lock only observable regression: signature, five names/order/bytes, `0644`/single-link outputs, changed-update success, normal success with no new sibling, and a public-safe error surface; defer its formal threat model to a separate change.
  Approved re-plan evidence: artifact quality review required the publication/idempotency and recovery/threat/legacy acceptance surfaces to be independently reviewable. Task 2.6 stayed open at re-plan time; the approved boundary narrows implementation but grants no lifecycle advancement, evidence currentness, or archive authority.
  Apply evidence: the review transaction uses `.review-pack-recovery-` plus 32 lowercase hex characters, retains only mechanically owned staging, makes identity uncertainty sticky with zero subsequent recovery mutation, treats any mkdir error as unowned observation-only uncertainty, classifies proven post-syscall promotion without rollback/downgrade, and blocks on every reserved-prefix sibling without automatic GC. Legacy signature/order/bytes/update/mode/link/public-safe behavior remains regression-locked; independent read-only security re-review returned Must Fix none, Should Fix none, Re-plan Needed no, Final Verdict pass.

## 3. Add bounded interfaces and public evidence

- [x] 3.1 Add `voice2task-data clean-boundary-review-pack` for explicit public bundle generation and `voice2task-data clean-boundary-review-lint --review-pack <name>` for one envelope relative to the fixed `data/local-private/clean-compiler-model-evaluation-boundary-v1/review-inputs/` root; do not create/modify that root, reject four raw input flags, return exit 0 only with `lint_conforms=true` plus `STRUCTURALLY_COMPLETE_REVIEW_REQUIRED` and all authorization/readiness flags false, omit generic `ok`, return nonzero with `lint_conforms=false` on incomplete/invalid/unsafe inputs, never call freeze/materialize/verify, and keep stdout/stderr free of values, paths, hashes, tracebacks, and raw exceptions.
  Apply evidence: added the two bounded commands without changing existing command behavior. Lint reads exactly one explicitly named envelope below the fixed review-input root with a 1 MiB descriptor-relative bound, strict JSON parsing, unsafe-name rejection before the reader, and fixed conservative failures for missing, redirected, exchanged, oversized, malformed, permission, and ordinary-error cases while leaving `BaseException` uncaught. Exit zero now requires exact equality with the complete canonical `STRUCTURALLY_COMPLETE_REVIEW_REQUIRED` truth and exact key surface; status/authority drift or added `ok`, protocol hash, or private-path keys normalize to fixed failure. Parser failures for both new commands discard raw argparse stderr and reject raw component flags before any read/write; generation calls the fixed writer exactly once, prints no paths, and performs zero private reads or lifecycle calls. The two related test files pass 387 cases, Ruff and focused Mypy pass, strict OpenSpec passes 15/15, and independent read-only review returned Must Fix none, Should Fix none, Re-plan Needed no, Final Verdict pass.
- [x] 3.2 Generate `reports/public-sample/clean-evaluation-acquisition-and-binding-review-pack-v1/`, verify deterministic double generation and manifest hashes, and prove the committed template plus every complete-envelope object/subobject fails unchanged at the execution freeze boundary while the archived S0 five-file evidence remains byte-identical.
  Apply evidence: after a complete absent-path/trusted-namespace preflight, the dedicated CLI performed one initial publish with rc 0, empty stderr, exactly seven builder-identical members, and zero recovery siblings. The same CLI was then invoked once more serially: the sealed first and second snapshots matched exactly for the final directory and all seven members across device, inode, type, UID, GID, mode, link count, size, mtime, and ctime (descriptor-read atime excluded), while bytes again matched the builder and recovery remained zero. The member SHA-256 values are `28c493815f8d7432c13997131c91de75101fb05e9b40d41ef30ad3f15c2813c1`, `a4d593600e0cd45500a28f4ab82a2a94b9dd3bbcd0374efae0886bcd3965e09a`, `8b7ca083846aae2bbf1df9b8b15734176400e4078bc39a44ba1df3433df5b533`, `ae40d6d3422c3a9f0068dd13d3b42bb1c9adc188168b431e5ecd1ef7285250e7`, `8c9b379f574341d1b8df05872fe66b319f6c2ea6d771d6c5dbd5ddb00d965acd`, `5d6669c8020bd5c4d231c5202783237927c364a22b5787aad58c0b393d7556e8`, and `e8e24e2e633d8cc7e4d43860051fcb7fc3c013ae2b813ba1050786dd22ea16df`; the manifest records the first six in order with exact byte counts and hashes and has no self-hash. The committed template and every dict subobject, plus every complete-envelope object/subobject covered by the execution-boundary regression, fail freeze unchanged with zero persistence. The S0 hashes remain exactly `24658fb7ca9143564133fa8521bed81b97f89b5cd94430560577def71ae85a13`, `9426602fe6fc768d569e1d7ceff78a2f5261a14fe4ca7c2e27391e19d7a08e25`, `84c775907f585a79d8d577077c4484e9648721b3a6d0619d01f912e092c0e8cc`, `19ea3d3b944faa3a1ef732db83d50e9affc59d4f4e0e231f5c6d980a81ade849`, and `e7b6dae68ba5dda941238d803e7387377e48bcdcc4d1e9f7786e118f5e68f0dc`; the private root is absent, ignored, untracked, and scoped-status clean, and recovery remains zero. The committed-artifact test passes, the focused group passes 5 cases, the two related files pass 388 cases, Ruff, focused Mypy, strict OpenSpec 15/15, and diff check pass, and independent read-only re-review returned Must Fix none, Should Fix none, Nice to Have none, Re-plan Needed no, Final Verdict pass. Progress is 13/20; task 3.3 remains unopened.
- [x] 3.3 Add a `DESIGN_ONLY` evidence-index entry and additive `CONTEXT.md` / current-status navigation describing `PREPARATION_ONLY`, 0/29 supplied, source unavailable, protocol not frozen, population not materialized, `human_acceptance_status=NOT_RECORDED`, and execution blocked; do not supersede or rewrite the archived S0 conclusion.
  Apply evidence: added one exact 11-key `DESIGN_ONLY` JSON item immediately after the archived S0 item and one first-row Design-Only Markdown entry, both classifying the public pack as `PREPARATION_ONLY` non-executable operator guidance over the canonical 29-field inventory with 0/29 supplied, 0 authoritatively bound, source `UNAVAILABLE`, review pack `READY_FOR_EXTERNAL_COMPLETION` only, candidate pack `INCOMPLETE`, protocol `NOT_FROZEN`, population `NOT_MATERIALIZED`, `human_acceptance_status=NOT_RECORDED`, and freeze/next/execution flags false. `CONTEXT.md` and current-status received only additive linked navigation and an explicit readiness-only claim boundary; the prior S0 JSON item, Blocked row, paragraph, bullet, table row, claim boundary, recommendation, and five artifact hashes remain unchanged, `BLOCKED`, current-claim false, and unsuperseded. The current-truth checker now guards the exact summary/index/navigation truth, required and leak-scan paths, source-document-relative Markdown links with controlled repository-escape rejection, exact default/legacy/new active-change allowlists, and exact-one review-pack/S0 IDs so duplicate or missing entries return fixed errors without dictionary overwrite or exceptions. RED moved from 13 failures to 24 passes, the stricter Explorer wording completed a separate 2-failure-to-2-pass cycle, and reviewer hardening completed a 4-failure/1-pass-to-5-pass cycle; the final evidence file passes 29 tests and the related project/review/boundary files pass 389 tests. The checker, leak scan, JSON/link checks, S0/private/recovery guards, Ruff, focused Mypy, strict OpenSpec 15/15, and diff check pass; independent read-only re-review returned Must Fix none, Should Fix none, Nice to Have none, Re-plan Needed no, Final Verdict pass. Progress is 14/20; task 3.4 remains unopened.
- [x] 3.4 Generate `docs/human-briefs/2026-07-11-prepare-clean-evaluation-acquisition-and-binding-review-pack-v1.html` from the OpenSpec, generated pack, tests, and validation evidence, explicitly distinguishing review-pack readiness from candidate review, human acceptance, binding, freeze, population materialization, row authoring, and experiment eligibility.
  Apply evidence: generated a concise Chinese Human Brief with an exact five-color, `max-width: 760px`, offline single-column UI design system; semantic `zh-CN` skeleton, skip link, visible focus, descriptive local links, mobile/print/reduced-motion rules, overflow wrapping, and no external resource, script, inline style, multi-column layout, gradient, or hidden authority surface. It preserves the exact active-apply preparation truth and explicitly separates `READY_FOR_EXTERNAL_COMPLETION` from candidate review/acceptance, authoritative binding, protocol freeze, population materialization, row authoring, experiment eligibility, and any model-improvement claim. The brief records one seven-member bundle whose manifest hashes exactly six payload members and does not self-hash, `focused_mypy_status=PASS`, the recorded full-Mypy baseline `39_ERRORS_IN_5_FILES`, and `full_mypy_rerun=PENDING_TASK_4_3`; it maps the still-pending 4.1 publication/recovery/CLI acceptance, 4.2 temporary public-data validation and protected-hash comparison, 4.3 full verification, 4.4 final independent review, and separately authorized 4.5 archive scopes without advancing them. The fail-closed HTML checker and 48-case evidence suite cover the exact skeleton, internal fragments, repository-relative/external/escape links, all forbidden resource tags and URL-bearing attributes, event handlers, exact palette, single-column CSS, required accessibility/media rules, unqualified Mypy markers, footer-scoped authority tokens, and active truth drift. Final verification passed 48 evidence-surface cases, 389 related review/boundary/project cases, the active-change truth checker, Ruff, focused Mypy, strict OpenSpec 15/15, diff check, and public leak scan with zero findings; independent read-only review completed PASS after its focused fixes. Browser viewport QA remains exactly `NOT_RUN_FILE_URL_BLOCKED`; the blocked `file://` route was not bypassed with localhost or any alternative server. Progress is 15/20; task 4.1 remains unopened.

## 4. Verify, review, and preserve the authorization boundary

- [x] 4.1 Run two explicit publication acceptance groups. Group A covers pre-existing trusted-root/ancestor/fixed-parent ownership, stable mode/identity, safe permissions, and no-ancestor-creation gates; root/ancestor/final/member symlink, nonregular, hardlink, and identity-exchange rejection; Darwin/Linux implementation/symbol preflight; fixed `REVIEW_PUBLICATION_NO_REPLACE_UNAVAILABLE` pre-staging failure; exact final/member bounded-read verification; initial `0700` staging, member fsyncs, retained-FD transition to verified `0755` immediately before promotion, directory/parent fsync, initial absent publish, exact deterministic second-generation no-op, zero reserved siblings on success/current, changed/partial rejection with no namespace, content, or capability-issued metadata mutation, post-preflight competitor preservation, filesystem no-replace failure, descriptor-read-atime-only exclusion, and proof that the review path never calls exchange/`unlinkat`/`rmdir`/destructive cleanup/ordinary fallback. Group B covers the reserved prefix and 128-bit token, transaction-owned FD/identity proof, verified FD restoration/retention at `0700`, fixed `REVIEW_PUBLICATION_RECOVERY_RETAINED`/`REVIEW_PUBLICATION_IDENTITY_UNCERTAIN`/`REVIEW_PUBLICATION_POST_SYSCALL_FAILURE`/`REVIEW_PUBLICATION_RECOVERY_PRESENT` statuses, observation-first post-syscall classification, no downgrade of a proven promoted final, verification that an uncertain object receives no further pathname, content, or capability-issued metadata mutation, the same descriptor-read-atime-only exclusion, current/task-3.2 blocking, no automatic GC, the explicit review same-EUID boundary, and legacy observable regression only (signature, five names/order/bytes, modes/link counts, changed-update success, normal-success no-new-sibling, public-safe errors). Also run in-memory-template/private-envelope lint, recursive unchanged envelope/draft/subobject execution rejection, no ephemeral-component serialization, complete-candidate ceiling, exact CLI exit/`lint_conforms`/no-`ok` output checks, zero-read/zero-write assertions, trusted review-input-root absence/symlink/exchange tests, schema conformance when the optional validator is available, manifest verification, and stdout/stderr leak tests covering missing files, permissions, size limits, invalid UTF-8, duplicate keys, deep JSON, unexpected OS errors, and forbidden canary-valued flags such as `--bindings /private/canary`.
  Apply evidence: acceptance exposed one true RED: an actual reserved-prefix sibling still let the current-truth checker return no error; the minimal fix added a read-only fail-closed recovery-name gate with fixed unavailable/current-blocked errors and no automatic GC. Direct adversarial coverage now locks unsafe or redirected trusted roots/ancestors/finals/members, bounded-read identity exchange, complete device/inode/UID/GID/mode/link/size/mtime/ctime comparison with descriptor-read atime as the only exclusion, Darwin/Linux implementation/symbol and exact no-replace flags, retained creation-FD recovery proof, sticky identity uncertainty with no later namespace/content/capability-issued metadata mutation, destructive/fallback prohibition, legacy observable-only behavior, and the CLI stdout/stderr canary matrix. Final acceptance passed Group A 39 cases, Group B 25 cases, the full CLI/lint file 193 cases, the evidence surface 55 cases, review plus boundary 418 cases, and the three related files 473 cases; focused reviewer fixes passed 8 cases and the final independent Task 4.1 review returned Must Fix none, Should Fix none, Final Verdict PASS. The active checker, Ruff, focused Mypy, `git diff --check`, strict OpenSpec 15/15, S0/private/recovery guards, and leak checks pass. The five S0 hashes remain `24658fb7ca9143564133fa8521bed81b97f89b5cd94430560577def71ae85a13`, `9426602fe6fc768d569e1d7ceff78a2f5261a14fe4ca7c2e27391e19d7a08e25`, `84c775907f585a79d8d577077c4484e9648721b3a6d0619d01f912e092c0e8cc`, `19ea3d3b944faa3a1ef732db83d50e9affc59d4f4e0e231f5c6d980a81ade849`, and `e7b6dae68ba5dda941238d803e7387377e48bcdcc4d1e9f7786e118f5e68f0dc`; the canonical private root remains absent, ignored, untracked, and scoped-status clean, and recovery siblings remain zero. Full pytest and full Mypy remain pending Task 4.3, so this preserves `ACTIVE APPLY`, `DESIGN_ONLY`, `PREPARATION_ONLY`, and execution blocked with no authority or lifecycle advancement. Progress is 16/20; task 4.2 remains unopened.
- [x] 4.2 Run the public dataset builder read-only-equivalent into a temporary output with `voice2task-data build-public --seed data/public-samples/seed_traces.jsonl --output <temporary-directory>`; run `voice2task-data validate --sft data/public-samples/sft_public_sample.jsonl --dpo data/public-samples/dpo_public_sample.jsonl --manifest data/public-samples/manifest_public_sample.json --public`, `voice2task-data dpo-check --dpo data/public-samples/dpo_public_sample.jsonl`, and `PYTHONPATH=src pytest -q tests/test_dataset_builder.py tests/test_schemas.py tests/test_dpo_validation.py tests/test_evaluator_reports.py`; compare protected public/lockbox/prompt/schema/evaluator hashes before and after.
  Apply evidence: a fresh temporary-directory build from the 247-row public seed completed with `public_safe=true`, 696 SFT rows, and 2100 DPO pairs, producing only the expected SFT, DPO, and manifest files; the temporary directory was then removed. Formal validation of the committed public sample returned `ok=true`, zero failures, 696 SFT rows, and 2100 DPO pairs, and formal `dpo-check` returned 2100 total pairs. The focused dataset/schema/DPO/evaluator acceptance passed 88 tests. Before/after SHA-256 comparison remained exact for all 15 protected files: four public sample inputs/outputs, five public lockbox aggregate artifacts, and six formatting/training/schema/evaluation/contract source files. The temporary manifest has intentionally dynamic `generated_at` and output-path values and therefore is not required to be byte-identical to the committed public manifest. The canonical private root remained absent, no repository/private/training/prediction/A100/experiment output was written, and the independent Task 4.2 review returned Must Fix none, Should Fix none, Final Verdict PASS. Progress is 17/20; task 4.3 remains unopened, full pytest and the recorded full-Mypy baseline rerun remain pending there, and task 4.5 still requires separate archive authorization.
- [x] 4.3 Run full `PYTHONPATH=src pytest -q`, `ruff check .`, focused Mypy plus the recorded full-Mypy baseline, `OPENSPEC_TELEMETRY=0 openspec validate --all --strict`, the active-change current-truth check, public leak scans, Human Brief link validation, JSON/manifest parsing, canonical private-root ignore/tracking/status guards, and `git diff --check`.
  Apply evidence: the full pytest command completed with the exact non-green result `1241 passed, 3 failed`; all three failures are confined to `tests/test_recovered_adapter_challenge_evaluation.py` and have the sole lifecycle reason `active_change:conflicting_active_changes` while this review-pack OpenSpec change remains active. Frozen challenge/policy hashes, 120 rows, template-disjointness, and the in-memory expected-current audit otherwise pass; this is analogous to the prior S0 archive-gate precedent and is not represented as a full-pytest PASS, because full green is expected only after separately authorized archive. Ruff passes. Focused Mypy passes 4 files with 0 issues; full Mypy truthfully remains 39 errors in 5 files across 31 checked source files, preserving the recorded error/file baseline while increasing coverage from the prior 28 files. The `src/voice2task/cli/data.py` findings are limited to legacy lines 478-609, and the new review routes at lines 364-389 are clean; full Mypy is not represented as PASS. Strict OpenSpec passes 15/15, the active-change current-truth checker passes, and the default zero-active checker remains the expected red until separately authorized archive. Leak scan reports 0 findings; Human Brief links plus the committed bundle pass 4 checks; strict JSON parsing passes 5/5; manifest verification passes 6/6 hashed payloads with no self-hash and 7/7 bundle members. The canonical private root remains absent, ignored, untracked, and scoped-status clean; recovery siblings remain zero; all five S0 hashes are unchanged; and `git diff --check` passes. The independent Task 4.3 gate review returned Must Fix none, Should Fix none, Final Verdict PASS. Browser viewport QA remains `NOT_RUN_FILE_URL_BLOCKED` and is not a Task 4.3 gate. Progress is 18/20; Task 4.4 remains unopened and archive still requires separate authorization.
- [x] 4.4 Complete independent read-only spec and code/security reviews; resolve every in-scope Must Fix and rerun affected gates while keeping real acquisition, authority approval, human acceptance, protocol freeze, private materialization, rows, arms, one-look, training, prediction, A100, experiments, and automatic next-change creation out of scope.
  Apply evidence: two final independent read-only reviews completed after resolving all 3/3 in-scope Must Fixes. First, post-staging `KeyboardInterrupt` and `SystemExit` now enter observation-first recovery while pre-staging BaseExceptions still propagate. Second, the private reader rejects FIFO/socket inputs before open, requires `O_NOFOLLOW | O_NONBLOCK`, and verifies linked/opened/post-read/relinked identity. Third, publication fixed codes are raised outside raw exception handlers with no raw context, cause, or canary; classifier faults return sanitized `REVIEW_PUBLICATION_IDENTITY_UNCERTAIN`. Focused regressions pass 7 BaseException cases, 6 private-reader cases, 7 adapter/CLI cases, and 4 clean-chain cases, for 24 targeted cases total; review plus boundary passes 434, evidence surface passes 55, and the three files pass 489. The current post-fix full-pytest checkpoint is exactly 1257 passed and 3 expected active-change archive-guard failures, still confined to `tests/test_recovered_adapter_challenge_evaluation.py` with sole reason `active_change:conflicting_active_changes`; it is not represented as full-pytest PASS. Ruff passes, focused Mypy remains 4 files / 0 issues, full Mypy remains 39 errors in 5 files across 31 checked source files, strict OpenSpec remains 15/15, the active truth checker passes while default zero-active truth remains expected red until separately authorized archive, and leak/link/strict-JSON/manifest/private/recovery/S0/diff guards remain unchanged and pass. The final spec/truth reviewer and code/security reviewer both returned Must Fix none, Should Fix none, Re-plan Needed no, Final Verdict PASS. `task_4_4_must_fixes_resolved=3/3`, `task_4_4_spec_truth_reviewer=PASS`, `task_4_4_code_security_reviewer=PASS`, and `final_independent_review_status=PASS_TASK_4_4`. Progress is 19/20; Task 4.5 is the only remaining task and still requires separate archive authorization. No archive, private input, freeze, materialization, row authoring, training, prediction, A100 execution, experiment, or automatic next change occurred.
- [x] 4.5 Archive only after the user separately authorizes archive and the Human Brief/evidence are current; then rerun the no-active-change truth checker, full pytest, strict OpenSpec, leak, protected-hash, private-boundary, link, manifest, and diff checks.
  Archive evidence: after explicit user authorization, the delta specification was synchronized into the new main capability specification with exactly 7 requirements and 29 scenarios; the main requirement/scenario tail is byte-for-byte equal to the archived delta, and a second sync assessment required no further change. The change was then moved to `openspec/changes/archive/2026-07-12-prepare-clean-evaluation-acquisition-and-binding-review-pack-v1`, leaving zero active OpenSpec changes. The archive-truth TDD cycle moved from 17 failures / 38 passes to 55 passes without adding test cases. The default no-active-change checker passes; the recovered-adapter challenge lifecycle suite passes 9 tests; strict OpenSpec passes 15/15; and the full repository suite passes 1260 tests with no failure scope or reason. The committed seven-member bundle/builder/freeze-boundary check passes; manifest truth remains 6/6 hashed payloads, no self-hash, and 7/7 members; Human Brief links resolve to the archive and synced main spec; and the explicit public leak scan reports zero findings. All 15 protected public/lockbox/source hashes and all five archived S0 hashes remain exact. The canonical private root remains absent, ignored, untracked, and scoped-status clean; reserved recovery siblings remain zero; and `git diff --check` passes. The lifecycle closeout preserves `DESIGN_ONLY`, `PREPARATION_ONLY`, source `UNAVAILABLE`, 0/29 supplied and authoritatively bound, human acceptance `NOT_RECORDED`, protocol `NOT_FROZEN`, population `NOT_MATERIALIZED`, freeze/next/execution flags false, archived S0 `BLOCKED` and unsuperseded, browser viewport QA `NOT_RUN_FILE_URL_BLOCKED`, and the recorded full-Mypy 39-errors-in-5-files baseline. No private input, freeze, materialization, row authoring, training, prediction, A100 execution, experiment, model-improvement claim, or automatic next change occurred. Progress is 20/20.
