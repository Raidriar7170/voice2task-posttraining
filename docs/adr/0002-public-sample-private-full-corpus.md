# Publish samples and builders, not the full local corpus

Voice2Task Post-Training will commit a small sanitized public sample dataset plus dataset builders, manifests, and aggregate reports. The full corpus remains local/private because it is derived from trace artifacts and augmentation inputs that may contain environment-specific or reviewer-irrelevant details.
