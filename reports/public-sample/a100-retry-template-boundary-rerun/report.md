# A100 retry template boundary train-split rerun

Status: prediction-only A100 rerun with the machine-only retry template boundary.

schema-valid recovery observed: final validated schema-valid output is `3/3` on this bounded train split.

exact match remains 0.0 and slot_f1 remains 0.0, so this is not a model-quality improvement claim or production-readiness evidence.

No training, checkpoint release, adapter release, parser relaxation, evaluator metric change, prediction repair, prediction re-score, semantic-equivalence scoring, slot normalization, public full-corpus release, or live-browser benchmark claim is made.
