# A100 retry template boundary diagnosis

schema-valid recovery observed on the bounded train split: `3/3` final validated outputs are schema-valid.

exact match remains 0.0, slot_f1 remains 0.0, and this is not a model-quality improvement claim.

The retry template boundary is visible in prediction metadata and prompt snapshots. Strict final metrics remain the source of truth.
